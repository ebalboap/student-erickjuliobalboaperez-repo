apiVersion: v1
kind: Service
metadata:
  name: student-erickbalboaeb-app-service
spec:
  selector:
    app: student-erickbalboaeb-app
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
